# ---------- web server ----------
flask>=3.0
flask-cors>=4.0

# ---------- OpenAI SDK ----------
openai>=1.16.0        # 1.16+ exposes beta.vector_stores

# ---------- env / config ----------
python-dotenv>=1.0

# ---------- PDF helper (optional but handy) ----------
pypdf>=4.2

# ---------- progress bar ----------
tqdm>=4.66
